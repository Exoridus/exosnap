# M1 Implementation Notes

## Scope completed

M1 established the repository engineering baseline:

- CMake project skeleton
- formatting/linting baseline
- reusable GoogleTest registration helper
- logging skeleton
- Windows CI baseline
- WinUI 3 app skeleton

## Work delivered

- **M1.1** — CMake skeleton and repo layout
- **M1.2** — formatting and linting baseline
- **M1.3** — test framework baseline with reusable GoogleTest registration helper
- **M1.4** — logging skeleton
- **logging-format follow-up fix** — formatting normalization of M1.4 logging files
- **M1.6** — Windows CI baseline
- **M1.5** — WinUI app skeleton with all eight navigation pages, dark theme, and build wiring

## Validation performed

The repository baseline was validated with:

- `pwsh -File scripts/pre-commit.ps1`
- debug configure/build/test
- release configure/build/test for CI baseline

The core C++ baseline passed all current tests.

The WinUI app structure, page list, dark-theme request, and build wiring were validated from source.

Local WinUI app build/launch was not completed because the local machine lacks the Visual Studio component:

`Microsoft.VisualStudio.ComponentGroup.WindowsAppSDK.Cpp`

CI was updated to install that component before build.

## Deviations and follow-up corrections

1. M1.2 required a one-time format normalization of M1.1 source files so the new formatter baseline could pass.
2. M1.4 required a follow-up formatting normalization because its new logging files initially violated the formatter baseline.
3. M1.5 required a repair pass to:
   - keep the CI build path viable for the newly added WinUI app,
   - remove a redundant `Microsoft.Common.props` import,
   - remove a mismatched Windows App SDK fallback import,
   - correct the WinUI C++ tooling component ID to:
     `Microsoft.VisualStudio.ComponentGroup.WindowsAppSDK.Cpp`

## Unresolved issues

- Local WinUI build/launch remains unverified until the required Visual Studio component is installed on the developer machine.
- GitHub Actions CI has been authored but has not yet been exercised on a remote repository because no remote exists yet.

## M1 exit assessment

M1 is complete enough to proceed to M2 capability probes. The remaining unresolved issues are tooling/environment validation gaps, not blockers for starting isolated M2 probes.
