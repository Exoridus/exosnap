name: CI

on:
  push:
  pull_request:

jobs:
  lint:
    name: lint
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt 6
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.9.0'
          arch: 'win64_msvc2022_64'
          modules: 'qtsvg'
          cache: true

      - name: Install cppcheck
        run: choco install cppcheck -y

      - name: Check clang-format
        run: pwsh -NonInteractive -File scripts/check-format.ps1

      - name: Configure (lint preset)
        run: cmake --preset windows-x64-lint

      - name: Build with static analysis
        run: cmake --build --preset windows-x64-lint

  build-test:
    name: build-test (${{ matrix.preset }})
    runs-on: windows-latest
    strategy:
      matrix:
        preset:
          - windows-x64-debug
          - windows-x64-release
      fail-fast: false

    steps:
      - uses: actions/checkout@v4

      - name: Install Qt 6
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.9.0'
          arch: 'win64_msvc2022_64'
          modules: 'qtsvg'
          cache: true

      - name: Configure (${{ matrix.preset }})
        run: cmake --preset ${{ matrix.preset }}

      - name: Build (${{ matrix.preset }})
        run: cmake --build --preset ${{ matrix.preset }}

      - name: Test (${{ matrix.preset }})
        run: ctest --preset ${{ matrix.preset }} --output-on-failure

      - name: Upload test logs (${{ matrix.preset }})
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ctest-logs-${{ matrix.preset }}
          path: build/${{ matrix.preset }}/Testing/Temporary/

      - name: windeployqt (release only)
        if: matrix.preset == 'windows-x64-release'
        run: |
          $exe = "build\windows-x64-release\Release\exosnap.exe"
          if (Test-Path $exe) {
            windeployqt --no-translations --no-opengl-sw $exe
          }
        shell: pwsh

      - name: Upload release artifact
        if: matrix.preset == 'windows-x64-release'
        uses: actions/upload-artifact@v4
        with:
          name: exosnap-release
          path: build\windows-x64-release\Release\
          if-no-files-found: ignore
